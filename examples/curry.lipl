
let mult = fun a b -> a * b end in

	let map = recfun map f xs ->
		if (empty xs) then
			[]
		else
			(f (head xs)) :: (map f (tail xs))
		end
	end
	in
		let multBy2 = (mult 2) in

			(map multBy2 [1, 2, 3, 4, 5])

		end
	end

end